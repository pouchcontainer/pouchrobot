package handler

/*import "github.com/sirupsen/logrus"
import "fmt"

// HandleEvent processes an event received from github
func HandleEvent(eventType string, data []byte) error {
	logrus.Info("")
	switch eventType {
	case "issues":
		processIssueEvent(data)
	case "pull_request":
		processPullRequestEvent(data)
	default:
		return fmt.Errorf("unknown event type %s", eventType)
	}
	return nil
}*/
